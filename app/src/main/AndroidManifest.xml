<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.KastorskayaKseniaAndreevna">

        <!-- Существующие Activity -->
        <activity android:name=".LinearActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".RelativeActivity" />
        <activity android:name=".ConstraintActivity" />
        <activity android:name=".FrameActivity" />

        <!-- Новые Activity (проверьте, что они есть) -->
        <activity android:name=".NameInputActivity" />
        <activity android:name=".SubjectInputActivity" />
        <activity android:name=".ScheduleInputActivity" />
        <activity android:name=".FragmentsDemoActivity" />

    </application>

</manifest>